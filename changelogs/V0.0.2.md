# V0.0.2 · 同步与验证契约修复

发布日期：2026-09-10

## 修复

- 同名个人模型清单覆盖内置清单，不再生成重复 slug；损坏目录返回明确错误，不覆盖文件。
- CPA 返回空模型列表时阻止同步，不再误报路由验证通过。
- 桌面桥接保留现有默认模型，仅显式指定时修改。
- 探针把待验证目录实际传入 Codex；拒绝不会被加载的替代 `--config` 文件，避免测错配置。

## 文档与验证

- 校准桌面透明模式、隔离 profile 和配置探测的使用说明及评测预期。
- 49 项自动化测试通过，覆盖同名覆盖、空路由、默认模型保持和探针参数传递。
- 真实 Codex Astra/Shell 探针通过，确认执行了实际命令。
- 不修改已有运行配置、不重启 CPA；跨供应商压缩状态迁移仍未实现。

[上一版本 V0.0.1](V0.0.1.md)
