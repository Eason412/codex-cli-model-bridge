# 贡献指南

欢迎提交配置兼容、模型清单、桥接修复和文档改进。一个 PR 聚焦一个问题，避免夹带个人配置或无关格式调整。

## 问题与复现

Bug 修复应提供：

- 问题表现、预期结果和实际结果。
- 系统、Python / Node.js、Codex、CPA 版本及使用模式（独立配置或透明代理）。
- 从最小配置开始的复现步骤、命令及脱敏错误片段。
- 相关 Issue；没有 Issue 时直接在 PR 中说明。

新功能说明使用场景与现有流程的缺口；纯文档变更说明原文的问题及修订依据。

## 解决思路

解释问题发生在哪一层、为什么采用当前方案，以及改动涉及哪些文件或行为。涉及配置写入、模型显示策略或服务启停时，说明对已有设置的影响和回退方法。

不要用只修改测试预期的方式掩盖行为回归。测试自身过期时，说明旧预期与当前设计的差异。

## 验证

按改动范围执行并记录命令与结果：

```sh
python3 -m unittest discover -s tests -v
node --check scripts/transparent_proxy.mjs
git diff --check
```

新增模型清单时还应提供：

- 精确模型 ID、系列和元数据来源。
- CPA 上游类型，例如 Coding Plan、订阅账号或插件；不提供账号身份和凭据。
- `/v1/models` 中的路由，以及对应模式下 `probe` 的脱敏结果。
- 若变更涉及工具或多代理兼容性，附对应工具探测结果。

真实探测会消耗上游额度，由贡献者在获授权环境中执行。未执行的验证说明原因，不填写为通过。

## 信息保护

提交前检查 diff、日志和截图，移除 API Key、Authorization、Cookie、OAuth 数据、私人路径和内网主机。使用 `example.com`、`<model-id>` 等示例值；测试认证数据应明确为假值。

个人模型偏好保存在仓外策略文件，不修改公共默认策略来适配单台机器。保留现有许可证与第三方署名。

PR 标题建议采用 `fix:`、`feat:`、`docs:` 或 `test:` 加简洁说明。请使用自动生成的 PR 模板完成提交说明。
